title: EncTimeWeb
description: EnTimeCode converts video with embedded LTC audio into video files with timecode metadata.
markdown: kramdown
plugins:
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
